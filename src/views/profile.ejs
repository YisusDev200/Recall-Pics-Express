<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ss</title>
    <%- include("partials/head") %>
  </head>
  <body>
    <%- include("partials/navigation") %>
    <div class="container p-4">
      <div class="row">
        <div class="col-md-4 offset-md-4">
          <div class="card animate__animated animate__fadeInDown">
            <img src="<%= image.path %>" class="card-img-top" />
            <div class="card-body">
              <h5 class="card-title"><%= image.title %></h5>
              <p class="card-text"><%= image.description %></p>
              <p class="card-text"><%= format(image.created_at) %></p>
              <div class="btn-group" role="group">
                <a
                  href="/image/<%= image.uuid %>/update"
                  class="btn btn-primary mx-1 rounded"
                  >Update Image</a
                >
                <a
                  href="/image/<%= image.uuid %>/delete"
                  class="btn btn-danger mx-1 rounded"
                  >Delete image</a
                >
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
